# Infrastructure

Platform infrastructure configuration and deployment resources.

## Structure

### `.github/workflows/`
Reusable GitHub Actions workflows for centralized CI/CD pipelines.

| Workflow | Description | Usage |
|----------|-------------|-------|
| `microservice-deploy.yml` | Reusable workflow for Java microservices deploying to **EKS** (Coolify deprecated) | Called by service repositories |

### `cicd/`
GitHub Actions reusable composite actions for CI/CD pipelines.

| Path | Description | Inputs | Outputs |
|------|-------------|--------|---------|
| `actions/gradle-build/` | Build, test, and package Gradle projects | See [table below](#gradle-build-inputs) | `build-status` |
| `actions/gradle-test/` | Run Gradle tests with coverage | See [table below](#gradle-test-inputs) | `test-status` |
| `actions/gradle-publish/` | Publish Gradle artifacts to GitHub Packages | See [table below](#gradle-publish-inputs) | - |
| `actions/maven-publish/` | Publish Maven artifacts to GitHub Packages | See [table below](#maven-publish-inputs) | - |

#### Gradle Build Inputs

| Input | Description | Default | Required |
|-------|-------------|---------|----------|
| `java-version` | Java version to use | `17` | No |
| `java-distribution` | Java distribution to use | `temurin` | No |
| `artifact-name` | Name for uploaded artifacts | `build-artifacts` | No |
| `artifact-path` | Path to upload artifacts from | `build/libs/` | No |
| `github-actor` | GitHub actor for package repository auth | - | No |
| `github-token` | GitHub token for package repository auth | - | No |

#### Gradle Test Inputs

| Input | Description | Default | Required |
|-------|-------------|---------|----------|
| `java-version` | Java version to use | `17` | No |
| `java-distribution` | Java distribution to use | `temurin` | No |
| `test-results-path` | Path to test results | `build/reports/tests/` | No |
| `artifact-name` | Name for uploaded test results | `test-results` | No |
| `github-actor` | GitHub actor for package repository auth | - | No |
| `github-token` | GitHub token for package repository auth | - | No |

#### Gradle Publish Inputs

| Input | Description | Default | Required |
|-------|-------------|---------|----------|
| `java-version` | Java version to use | `17` | No |
| `java-distribution` | Java distribution to use | `temurin` | No |
| `release-version` | Release version (empty = SNAPSHOT) | - | No |
| `github-actor` | GitHub actor | - | **Yes** |
| `github-token` | GitHub token with `packages:write` | - | **Yes** |

#### Maven Publish Inputs

| Input | Description | Default | Required |
|-------|-------------|---------|----------|
| `java-version` | Java version to use | `17` | No |
| `java-distribution` | Java distribution to use | `temurin` | No |
| `release-version` | Release version (empty = SNAPSHOT) | - | No |
| `github-actor` | GitHub actor | - | **Yes** |
| `github-token` | GitHub token with `packages:write` | - | **Yes** |

### `datastore/`
Database and cache infrastructure definitions per environment.

| Path | Description |
|------|-------------|
| `local/` | Docker Compose for local development (PostgreSQL, Redis) |
| `dev/`, `stg/`, `prod/` | Environment-specific infrastructure configs |

## Secret Conventions

When using the reusable workflows and actions, the following secrets should be configured at the **organization level** or **repository level**:

| Secret | Description | Required By |
|--------|-------------|-------------|
| `GITHUB_TOKEN` | Auto-generated by GitHub Actions, used for package registry auth | All publish actions |
| `GPR_USERNAME` | GitHub username for GitHub Packages access | Gradle/Maven builds |
| `GPR_TOKEN` | Personal Access Token with `packages:write` scope | Gradle/Maven publish |

### EKS Deployment Configuration (microservice-deploy.yml)

When using the `microservice-deploy.yml` workflow, services must configure:

| Setting | Type | Example for `platform-user-service` | Description |
|---------|------|-------------------------------------|-------------|
| `AWS_ROLE_ARN` | **Repository Variable** | `arn:aws:iam::692046683886:role/serenity-github-actions-role` | IAM role for GitHub Actions OIDC authentication |

**Required workflow permissions:**
```yaml
permissions:
  id-token: write
  contents: read
  packages: write
```

### Coolify Deployment Secrets (Deprecated)

Coolify deployment is being migrated to EKS. These secrets are maintained for backward compatibility during the transition:

| Secret Pattern | Example | Description |
|----------------|---------|-------------|
| `COOLIFY_URL_{ENV}` | `COOLIFY_URL_DEV` | Coolify instance URL |
| `COOLIFY_API_TOKEN_{ENV}` | `COOLIFY_API_TOKEN_DEV` | Coolify API token |
| `COOLIFY_APP_UUID_{SERVICE}_{ENV}` | `COOLIFY_APP_UUID_PLATFORM_USER_SERVICE_DEV` | Application UUID in Coolify |
| `SERVICE_URL_{SERVICE}_{ENV}` | `SERVICE_URL_PLATFORM_USER_SERVICE_DEV` | Service public URL for health checks |

**Secret naming transformation:** Service name is converted to uppercase with underscores:
- `platform-user-service` → `PLATFORM_USER_SERVICE`

### Required GitHub Token Permissions

For workflows that publish artifacts, ensure the token has these permissions:

```yaml
permissions:
  contents: read
  packages: write
  actions: read
```

## Quick Start

### Using Reusable Workflows

#### microservice-deploy.yml

Service repositories can call the centralized deployment workflow:

```yaml
name: Deploy

on:
  push:
    branches: [main, master]
    tags: ['v*']
  pull_request:
    branches: [main, master]
  workflow_dispatch:
    inputs:
      environment:
        description: 'Deployment environment'
        required: true
        default: 'development'
        type: choice
        options:
          - development
          - production

jobs:
  deploy:
    uses: serenitiflow/infrastructure/.github/workflows/microservice-deploy.yml@main
    with:
      service-name: platform-user-service
      java-version: '17'
      aws-role-arn: ${{ vars.AWS_ROLE_ARN }}
    secrets: inherit
```

**Inputs:**

| Input | Description | Default | Required |
|-------|-------------|---------|----------|
| `service-name` | Service identifier (kebab-case) | - | **Yes** |
| `java-version` | Java version for Gradle build | `17` | No |
| `java-distribution` | JDK distribution | `temurin` | No |
| `health-check-path` | Health check endpoint path | `/actuator/health` | No |
| `info-endpoint-path` | Version info endpoint path | `/actuator/info` | No |
| `deploy-timeout-minutes` | Deployment wait timeout | `10` | No |
| `health-check-timeout-minutes` | Health check timeout | `5` | No |
| `eks-cluster-name` | Shared EKS cluster name | `serenity-shared-cluster` | No |
| `eks-namespace-dev` | Kubernetes namespace for dev | `dev-serenity` | No |
| `eks-namespace-prod` | Kubernetes namespace for prod | `prod-serenity` | No |
| `eks-aws-region` | AWS region for EKS | `eu-central-1` | No |
| `aws-role-arn` | AWS IAM role ARN for GitHub Actions OIDC | - | **Yes** |

**Jobs:**
- `validate` - Validates required configuration
- `build` - Builds and tests with Gradle
- `build-image` - Builds and pushes Docker image to GHCR
- `deploy-dev` - Deploys to Coolify development environment *(deprecated)*
- `deploy-prod` - Deploys to Coolify production environment (tag pushes only) *(deprecated)*
- `deploy-dev-eks` - Deploys to EKS development environment
- `deploy-prod-eks` - Deploys to EKS production environment (tag pushes only)

### Using Composite Actions

```yaml
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: serenitiflow/infrastructure/cicd/actions/gradle-build@main
        with:
          java-version: '17'
          github-actor: ${{ github.actor }}
          github-token: ${{ secrets.GITHUB_TOKEN }}
```

### Local Development

Start local dependencies:

```bash
cd datastore/local
docker compose up -d
```

Stop local dependencies:

```bash
docker compose down
```

## See Also

- [CI/CD README](cicd/README.md) - Detailed pipeline documentation
- [Local Datastore README](datastore/local/README.md) - Local development setup
- [GitHub Actions Documentation](https://docs.github.com/en/actions) - Official docs
